import type { DefaultEdgeOptions, EdgeTypes } from "reactflow";

export const edgeTypes: EdgeTypes = {};

export const defaultEdgeOptions: DefaultEdgeOptions = {
  type: "default",
  animated: false,
  style: { strokeWidth: 2 },
};
